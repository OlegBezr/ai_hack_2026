# Copy to .env.local and fill in.
# Local dev: run `supabase start` in the repo root and use the printed values.
# Production: your project URL + the *publishable* key (never the service role key).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
