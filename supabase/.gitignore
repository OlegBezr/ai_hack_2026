# Supabase
.branches
.temp

# dotenvx
.env.keys
.env.local
.env.*.local

# Edge function secrets (auto-loaded by `supabase start`)
functions/.env

# Remote deploy secrets (pushed via `supabase secrets set --env-file`)
.env.prod
