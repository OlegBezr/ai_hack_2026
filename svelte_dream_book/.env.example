# Local Supabase stack (output of `supabase status`). Anon key is safe to expose
# to the browser; it only grants RLS-scoped access.
PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
PUBLIC_SUPABASE_ANON_KEY=your-local-anon-key
