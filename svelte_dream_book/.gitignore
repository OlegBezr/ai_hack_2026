node_modules
/build
/.svelte-kit
/.vercel
/package
.env
.env.*
!.env.example
# Safe to commit: only PUBLIC_ Supabase vars (anon key is RLS-scoped, meant for
# the browser). Lets Vercel's Git build read them with no dashboard config.
!.env.production
vite.config.ts.timestamp-*
.DS_Store

# Sentry Config File
.env.sentry-build-plugin
