// Run as a client-side SPA: the reader is highly interactive and the typed
// Supabase client persists its session in the browser. (Swap to @supabase/ssr +
// server load functions if you later want SSR / cookie auth.)
export const ssr = false;
export const prerender = false;
